name: Provision GKE Cluster and Deploy Manifests

on:
  push:
    branches:
      - developer

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      REGION: ${{ secrets.GCP_REGION_HML }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up GCP credentials (from base64)
        run: |
          mkdir -p /home/runner/gcp_credentials
          echo "${{ secrets.GCP_CREDENTIALS_BASE64_HML }}" | base64 -d > /home/runner/gcp_credentials/credentials.json
          chmod 600 /home/runner/gcp_credentials/credentials.json

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_wrapper: false

      - name: Terraform Init (Cluster + IP)
        run: |
          cd terraform
          terraform init \
            -var="project_id=${{ secrets.GCP_PROJECT_ID_HML }}" \
            -var="region=${{ secrets.GCP_REGION_HML }}" \
            -var="credentials_file_path=/home/runner/gcp_credentials/credentials.json"

      - name: Terraform Import existing static IP (optional)
        run: |
          cd terraform
          export TF_VAR_project_id="${{ secrets.GCP_PROJECT_ID_HML }}"
          export TF_VAR_region="${{ secrets.GCP_REGION_HML }}"
          export TF_VAR_credentials_file_path="/home/runner/gcp_credentials/credentials.json"

          terraform import google_compute_address.ingress_ip \
            projects/${{ secrets.GCP_PROJECT_ID_HML }}/regions/${{ secrets.GCP_REGION_HML }}/addresses/ingress-static-ip || true

      - name: Terraform Refresh (sync imported IP)
        run: |
          cd terraform
          terraform refresh \
            -var="project_id=${{ secrets.GCP_PROJECT_ID_HML }}" \
            -var="region=${{ secrets.GCP_REGION_HML }}" \
            -var="credentials_file_path=/home/runner/gcp_credentials/credentials.json"

      - name: Terraform Apply (Cluster + IP)
        run: |
          cd terraform
          terraform apply -auto-approve \
            -target=google_container_cluster.primary \
            -target=google_compute_address.ingress_ip \
            -var="project_id=${{ secrets.GCP_PROJECT_ID_HML }}" \
            -var="region=${{ secrets.GCP_REGION_HML }}" \
            -var="credentials_file_path=/home/runner/gcp_credentials/credentials.json"

      - name: Terraform Apply (Ingress Helm)
        run: |
          cd terraform
          terraform apply -auto-approve \
            -var="enable_ingress=true" \
            -var="project_id=${{ secrets.GCP_PROJECT_ID_HML }}" \
            -var="region=${{ secrets.GCP_REGION_HML }}" \
            -var="credentials_file_path=/home/runner/gcp_credentials/credentials.json"

      - name: Extract Cluster Name
        run: |
          cd terraform
          CLUSTER_NAME=$(terraform output -raw cluster_name | tr -d '\r')
          echo "CLUSTER_NAME=$CLUSTER_NAME" >> $GITHUB_ENV

      - name: Configure kubectl manually (secure & plugin-free)
        run: |
          gcloud auth activate-service-account --key-file=/home/runner/gcp_credentials/credentials.json
          gcloud config set project ${{ secrets.GCP_PROJECT_ID_HML }}

          ACCESS_TOKEN=$(gcloud auth print-access-token)
          CLUSTER_ENDPOINT=$(gcloud container clusters describe $CLUSTER_NAME \
            --region $REGION \
            --format="value(endpoint)")

          CLUSTER_CA=$(gcloud container clusters describe $CLUSTER_NAME \
            --region $REGION \
            --format="value(masterAuth.clusterCaCertificate)")

          echo "$CLUSTER_CA" | base64 -d > /tmp/ca.crt

          kubectl config set-cluster gke-cluster \
            --server=https://$CLUSTER_ENDPOINT \
            --certificate-authority=/tmp/ca.crt

          kubectl config set-credentials gke-user --token=$ACCESS_TOKEN
          kubectl config set-context gke-context \
            --cluster=gke-cluster --user=gke-user
          kubectl config use-context gke-context

      - name: Download Kubernetes manifests from GCS
        run: |
          mkdir -p k8s-manifests
          gsutil -m cp -r gs://blog-iris-hml/manifests/* ./k8s-manifests

      - name: Apply namespaces first
        run: |
          kubectl apply --validate=false -f ./k8s-manifests/namespace
          sleep 5

      - name: Apply remaining manifests
        run: |
          find ./k8s-manifests -type f -name "*.yaml" ! -name "ingress.yaml" ! -name "ingress-final.yaml" | while read file; do
            echo "Applying $file"
            kubectl apply -f "$file"
          done
